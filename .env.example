# Gemini AI API Key
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: Specify the port for the FastAPI server (default is usually 8000)
PORT=8000

# Optional: Specify the host for the FastAPI server (default is usually 0.0.0.0)
HOST=0.0.0.0